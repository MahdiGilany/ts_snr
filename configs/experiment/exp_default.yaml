# @package _global_

# defaults:
  # - 
  
name: "setting_up_deeptime"
seed: 0
batch_size: 32
epochs: 50

### uncomment to change the defaults ###
trainer:
  check_val_every_n_epoch: 5

# model:
#   model_name: nbeats
#   input_chunk_length: 24
#   output_chunk_length: 12

# data:
#   noise_mean: 0
  # noise_std: 0

# callbacks: 
#   model_checkpoint:
#     every_n_epochs: 5

logger:
  wandb:
    project: ts_snr
    # entity: borealisai