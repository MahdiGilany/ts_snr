# @package _global_

# defaults:
  # - 
  
name: "nbeats_airpassenger"
seed: 0
batch_size: 32
epochs: 100

### uncomment to change the defaults ###
trainer:
  check_val_every_n_epoch: 1

# model:
#   model_name: nbeats
#   input_chunk_length: 24
#   output_chunk_length: 12

# data:
  # dataset_name: air_passengers
  # noise_mean: 0
  # noise_std: 0

# callbacks: 
#   model_checkpoint:
#     every_n_epochs: 5
#   early_stopping:
#     patience: 10

logger:
  wandb:
    project: ts_snr
    entity: borealisai