# @package _global_

defaults:
  - exp_default
  
name: "test_different_optimizers" #"nbeats_10ep_etth2_target_6_test_scheduler"
epochs: 100
# batch_size: 256
seed: 1234
save_checkpoints: true

trainer:
  check_val_every_n_epoch: 1

data:
  dataset_name: air_passengers
  split_ratio: [0.6, 0.2, 0.2]
  noise_std: 0.0 # 0.1
  # target_series_index: 6

model:
  model_name: deeptime
  output_chunk_length: 6 #96
  # input_chunk_length: 672

# callbacks:
#   model_checkpoint:
#     every_n_epochs: 5
#   early_stopping:
#     patience: 50

new_dir: True

# logger: null

verbose: True