# @package _global_

defaults:
  - exp_default
  - override driver: twostage_globalforecasting_driver
  
name: twostage_deeptime_exchange_layersz256
epochs: 100
batch_size: 256
seed: 0
save_checkpoints: true

trainer:
  check_val_every_n_epoch: 1
  num_sanity_val_steps: 0
  # gradient_clip_val: 10.0

data:
  dataset_name: exchange_rate
  # split_ratio: [0.15, 0.05, 0.05]
  noise_std: 0. # 0.1
  target_series_index: -2
  # prct_rows_to_load: .1

model:
  model_name: deeptime
  output_chunk_length: 96
  input_chunk_length: 288
  # K: 256
  # omp_tolerance: 0.001
  # n_nonzero_coefs: 45
  # layer_size: 256

# callbacks:
#   model_checkpoint:
#     every_n_epochs: 5
  # early_stopping:
  #   patience: 50

new_dir: False
# train_stage1: False

# logger: null

verbose: True