# @package _global_

defaults:
  - exp_default
  
name: "nbeats_etth1_test"
epochs: 1
save_checkpoints: True

trainer:
  check_val_every_n_epoch: 1
  
data:
  dataset_name: etth1

# model:
#   model_name: deeptime

# logger: null