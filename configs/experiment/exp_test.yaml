# @package _global_

defaults:
  - exp_default

name: deeptime_crypto_Bitcoin_pcrt.1_univar_noise0._in288_out15
epochs: 100
batch_size: 256
seed: 0
save_checkpoints: true

trainer:
  check_val_every_n_epoch: 1
  num_sanity_val_steps: 0
  # gradient_clip_val: 10.0

data:
  dataset_name: crypto
  # split_ratio: [0.15, 0.05, 0.05]
  noise_std: 0. # 0.1
  noise_type: laplace
  target_series_index: -1
  prct_rows_to_load: .1
  crypto_name: Bitcoin
  # chunk_number: 0

model:
  model_name: deeptime
  output_chunk_length: 15
  input_chunk_length: 288
  # K: 100
  # omp_tolerance: 0.001
  # n_nonzero_coefs: 45
  # layer_size: 256
  # out_features: 256
  # inr_layers: 5
  # optimizer_kwargs: 
  #   lr: 0.0001

# callbacks:
#   model_checkpoint:
#     every_n_epochs: 5
  # early_stopping:
    # patience: 50

new_dir: True

# logger: null

verbose: True