# @package _global_

defaults:
  - exp_default
  
name: support_drpout_keep0.05_deeptime_etth2_noise0._laplace_layersize256_in288_out96_dictreg0.01_patience25
epochs: 100
batch_size: 256
seed: 0
save_checkpoints: true

trainer:
  check_val_every_n_epoch: 1
  num_sanity_val_steps: 0
  # gradient_clip_val: 10.0

data:
  dataset_name: etth2
  # split_ratio: [0.15, 0.05, 0.05]
  noise_std: 0. # 0.1
  noise_type: laplace
  target_series_index: -1
  # prct_rows_to_load: .003
  # crypto_name: Bitcoin

model:
  model_name: deeptime
  output_chunk_length: 96
  input_chunk_length: 288
  # K: 100
  # omp_tolerance: 0.001
  # n_nonzero_coefs: 45
  # layer_size: 256
  # out_features: 256
  # inr_layers: 5
  # optimizer_kwargs: 
  #   lr: 0.0001

callbacks:
#   model_checkpoint:
#     every_n_epochs: 5
  early_stopping:
    patience: 25

new_dir: True

# logger: null

verbose: True